export type Props = {
  /**
   * The layout of the skeleton.
   * @default 'box'
   */
  layout?: 'text' | 'box';
  /**
   * The width of the skeleton.
   * @default '100%'
   */
  width: string;
  /**
   * The height of the skeleton.
   * @default '100%'
   */
  height: string;
};
